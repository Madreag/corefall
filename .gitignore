# Transient / log
*.log
*.tmp
*.pid

# Editor swap / backup
*.swp
*.swo
.*.swp
.*.swo
*~
*.bak
*.orig
*.rej
.netrwhist

# Secrets / env (do NOT weaken this — see corefall AGENTS.md "Secrets Posture")
.env
.env.*
!.env.example
.envrc

# Rust / Cargo
target/
**/*.rs.bk

# Python (game/tools/ vendors prototype_run_check.py; future tools may grow)
__pycache__/
*.pyc
*.pyo
*.pyd
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
pip-log.txt
.coverage
coverage/
htmlcov/

# Profiling / debugging artifacts
flamegraph.svg
perf.data
perf.data.old
*.profraw
*.profdata

# Generated prototype evidence (run bundles are gitignored — keep README marker only)
prototype_runs/*
!prototype_runs/native/
prototype_runs/native/*
!prototype_runs/native/README.md
# BP closure artifacts — the AI-Agent Self-Test Report + BP Goal Coverage Report
# live inside `bp<N>_<UTC>_<hash>/notes.md` and are the source-of-truth closure
# evidence per `corefall/AGENTS.md` Build Point Closure Gate. Force-include the
# notes.md only; the rest of bp<N>_* (events.jsonl, summary.json, captures/)
# stays gitignored alongside every other bundle.
!prototype_runs/native/bp[0-9]*_*/
prototype_runs/native/bp[0-9]*_*/*
!prototype_runs/native/bp[0-9]*_*/notes.md
!prototype_runs/native/bp[0-9]*_*/grading.json
# LLM-graded test verdicts produced for fun-proof bundles (BP2 onward) per
# `.claude/skills/corefall-review/SKILL.md` §LLM-Graded Test Verdicts Gate.
# Only the grading.json itself is closure evidence; the rest of the bundle
# (events.jsonl, summary.json, captures/, frame_*.png) is too large + churns
# too fast for git. Force-include grading.json in any milestone-prefixed
# bundle (m1.5_*, m2_*, m2.5_*, m3a_*, etc.) so reviewers can audit the
# LLM grades offline without re-running the scenario. .gitignore needs the
# parent dir to be un-ignored first; we whitelist the parent + exclude its
# contents + re-allow grading.json — same pattern as the bp[0-9]* block above.
!prototype_runs/native/m[0-9]*_*/
prototype_runs/native/m[0-9]*_*/*
!prototype_runs/native/m[0-9]*_*/grading.json
run_bundles/
captures/
screenshots/

# IDE / editor / tool
.idea/
.vscode/
*.iml
.fleet/
.zed/
.cursor/

# Claude Code per-machine local state (project-shared skills under .claude/skills/ stay tracked)
.claude/settings.local.json
.claude/cache/
.claude/local/

# macOS
.DS_Store
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.fseventsd

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux
.Trash-*
.directory
