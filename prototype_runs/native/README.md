# Native Prototype Runs

This directory is the standard output root for generated run bundles.

Implementation agents should write milestone evidence here using the canonical run-bundle naming convention from:

```text
docs/plan/spec/prototype-roadmap.md
```

Generated run bundles are ignored by git. Commit only durable summaries in `docs/implementation-log/` and canonical checklist/roadmap updates in the vault.
