{
  "scenario": "m1_actor_range",
  "tick_rate_hz": 60,
  "description": "M4 § Acceptance: dense control + fire burst designed to exceed any reasonable per-tick recorder capacity. When run under a small ring buffer (Recorder::with_capacity(100)), cosmetic events drop first under pressure; the recorder's dropped_total surfaces in summary.json.event_counts.dropped_total and the canonical checker verifies the priority discipline (no gameplay event drops while cosmetic events remain).",
  "steps": [
    { "method": "scenario.reset", "params": {} },
    { "method": "act.player.move", "params": { "x": 1.0, "y": 0.0 } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 60 } },
    { "method": "act.player.fire", "params": { "pressed": true } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 4 } },
    { "method": "act.player.fire", "params": { "pressed": false } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 1 } },
    { "method": "act.player.fire", "params": { "pressed": true } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 4 } },
    { "method": "act.player.fire", "params": { "pressed": false } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 1 } },
    { "method": "act.player.fire", "params": { "pressed": true } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 4 } },
    { "method": "act.player.fire", "params": { "pressed": false } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 1 } },
    { "method": "act.player.fire", "params": { "pressed": true } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 4 } },
    { "method": "act.player.fire", "params": { "pressed": false } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 1 } },
    { "method": "act.player.reload", "params": {} },
    { "method": "sim.run_for_ticks", "params": { "ticks": 120 } },
    { "method": "observe.once", "params": {} },
    { "method": "runbundle.write", "params": {} }
  ]
}
