{
  "scenario": "micro_reactor_defense",
  "tick_rate_hz": 60,
  "description": "M9 player-dies-first path: player walks into the guard's LOS, stays still; the guard kills the player before the reactor reaches 0 HP. Mission resolves Lost(PlayerDead) with the reactor still alive.",
  "steps": [
    { "method": "scenario.reset", "params": {} },
    { "method": "sim.run_for_ticks", "params": { "ticks": 60 } },
    { "method": "act.player.aim", "params": { "x": 1.0, "y": 0.0 } },
    { "method": "act.player.move", "params": { "x": 1.0 } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 600 } },
    { "method": "act.player.move", "params": { "x": 0.0 } },
    { "method": "sim.run_for_ticks", "params": { "ticks": 1800 } }
  ]
}
