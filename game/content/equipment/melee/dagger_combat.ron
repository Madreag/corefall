// **M6C** SKU `dagger_combat` in category `melee`. Generated by
// `cargo run --bin generate_m6c_rons` from
// `cf_equipment::m6c_entries_by_category()`. Re-generate after registry edits.

(
  id: "dagger_combat",
  display_name: "Combat Dagger",
  mass_kg: 0.2,
  dimensions: (
    w: 1,
    h: 2,
  ),
  bulk_volume_l: 0.4,
  stackable: false,
  max_stack: 1,
  category: weapon,
  container_capacity: None,
  liquid_capacity_l: None,
  rotation_allowed: true,
  quick_slot_eligible: true,
  durability_max: Some(600),
  repair_recipe: None,
  material_weight_breakdown: {
    "steel": 0.2,
  },
  crafting_yield_count: 1,
  origin_compatibility: [],
  forbid_for_origin: [],
)
