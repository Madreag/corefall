(
  schema_version: 1,
  id: "m14e_support_beam_save",
  display_name: "M14E Support Beam Save",
  description: "M14E Gherkin-4 + Gherkin-5 + Gherkin-7 — support_beam anchor + perf-budget fixture. The middle chunk (0,0) hosts a 32-px-wide tunnel with a pre-anchored support_beam (`anchored=true`), so the integrity pass + cave-in roll never fire there. A bordering 12-px tunnel verifies the ≤16-px floor invariant (cave-in roll = 0). Round-trips through SaveBlob produce identical checksums on save → load → save. The perf-test fixture drives 500 chunks (m14e_tunnel_spans replicated) to assert the ≤0.4 ms p99 budget.",
  seed: 14,
  duration_ticks: Some(600),
  region: (anchor: (0.0, 0.0), width: 1024.0, height: 1024.0),
  gravity: -980.0,
  floor_y: 16.0,
  teams: [],
  actors: [
    (id: 1, team: "blue", spawn: (50.0, 32.0), controllable: true, hp: 100.0,
      inventory: (rifle: None), half_extents: Some((8.0, 16.0)), aim: Some((1.0, 0.0)),
      origin_id: Some("human")),
  ],
  objectives: [],
  director: None,
  capabilities: (debug: false, control_api: true, save_load: true),
  save_fields: [],
  expected_tests: ["M14E-BEAM-SAVE-01", "M14E-PERF-01"],
  notes: "M14E support_beam save fixture. The (0,0) chunk is anchored — the per-tick collapse-check pass observes `anchored=true` and skips the cave-in roll, keeping the chunk locked at effective integrity 500 for the entire 600-tick window. The 12-px chunk verifies the unsupported-span floor (no roll, no L1). Round-trip determinism: same seed → identical checksum at tick 600.",
  scripted_steps: [],
  m14d_projectile_pool: [],
  m14d_replay_intercepts: false,
  m14e_tunnel_spans: [
    (
      id: "supported_32_px",
      chunk_id: (0, 0),
      bbox_min: (64, 60),
      bbox_max: (96, 76),
      unsupported_span_px: 32,
      ceiling_thickness_px: 4,
      vibration_modifier: 1.0,
      anchored: true,
      cascade_neighbors: [],
      damage_actor_id: None,
    ),
    (
      id: "sub_threshold_12_px",
      chunk_id: (1, 0),
      bbox_min: (320, 60),
      bbox_max: (332, 76),
      unsupported_span_px: 12,
      ceiling_thickness_px: 4,
      vibration_modifier: 1.0,
      anchored: false,
      cascade_neighbors: [],
      damage_actor_id: None,
    ),
  ],
  m14e_cave_in_seed_offset: 11,
)
