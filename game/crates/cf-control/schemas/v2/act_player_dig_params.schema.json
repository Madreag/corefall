{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ActPlayerDigParams",
  "description": "`act.player.dig` — M1.5 soft-breach dig request. `target` picks an explicit\nbreach id; `None` means dig the nearest in-range strip.",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "target": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "schema_version"
  ]
}
