{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SrvDumpSquadStateParams",
  "description": "view (verb registry + formation catalog + archetype-BT node counts +\nper-squad state row). Used by the M25 wheel + Tab overlay + assert\nthe spec's \"verb registry enumerates 50+ named squad commands\"\ninvariant.",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "squad_id": {
      "type": "integer",
      "format": "uint64",
      "default": 0,
      "minimum": 0
    }
  },
  "additionalProperties": false,
  "required": [
    "schema_version"
  ]
}
