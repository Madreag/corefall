{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ActPlayerReleaseRopeParams",
  "description": "**M14J** § `act.player.release_rope` — release the embedded grapple\nrope; actor inherits the pendulum velocity at the instant of release.",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    }
  },
  "additionalProperties": false,
  "required": [
    "schema_version"
  ]
}
