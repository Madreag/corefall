{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ActPlayerResetParams",
  "description": "`act.player.reset` — return the player to their spawn with full HP / ammo.",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    }
  },
  "additionalProperties": false,
  "required": [
    "schema_version"
  ]
}
