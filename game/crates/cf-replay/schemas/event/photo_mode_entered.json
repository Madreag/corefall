{
  "$schema": "http://json-schema.org/draft-2020-12/schema",
  "$id": "cf-replay/event/photo_mode_entered.v1",
  "title": "photo_mode.entered",
  "description": "Emitted when the player enters photo mode (F12). Sim pauses; cf-photo's PhotoModeState.active becomes true.",
  "type": "object",
  "required": ["filter"],
  "properties": {
    "filter": { "type": "string", "enum": ["none", "sepia", "black_and_white", "color_grade", "cyberpunk_neon"] }
  }
}
