{
  "$schema": "http://json-schema.org/draft-2020-12/schema",
  "$id": "cf-replay/event/photo_mode_shot_taken.v1",
  "title": "photo_mode.shot_taken",
  "description": "Emitted when the player captures a photo. The PNG is exported via cf-photo::export_png; this event records the metadata.",
  "type": "object",
  "required": ["shot_index", "filter"],
  "properties": {
    "shot_index": { "type": "integer", "minimum": 1 },
    "filter": { "type": "string" },
    "path": { "type": ["string", "null"] }
  }
}
