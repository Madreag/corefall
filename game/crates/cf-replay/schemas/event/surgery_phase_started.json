{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://corefall/event/surgery.phase_started.v0.1",
  "title": "surgery.phase_started",
  "description": "M14H § Surgery 5-phase minigame — fires on entry into each phase (Open / Diagnose / Operate / Close / Recover). duration_seconds is the phase's authored runtime.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "schema_version": { "const": "prototype-recorder-event.v0.1" },
    "category": { "const": "surgery" },
    "event_type": { "const": "phase_started" },
    "actor_id": { "type": ["integer", "null"] },
    "tick": { "type": "integer", "minimum": 0 },
    "payload": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "actor_id": { "type": "integer" },
        "tick": { "type": "integer", "minimum": 0 },
        "phase": {
          "type": "string",
          "enum": ["Open", "Diagnose", "Operate", "Close", "Recover"]
        },
        "duration_seconds": { "type": "number", "minimum": 0.0 }
      },
      "required": ["actor_id", "tick", "phase", "duration_seconds"]
    }
  },
  "required": ["schema_version", "category", "event_type", "tick", "payload"]
}
