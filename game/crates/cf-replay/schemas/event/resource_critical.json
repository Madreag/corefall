{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://corefall/event/resource.critical.v0.1",
  "title": "resource.critical",
  "description": "M17 § resource.* family — an actor resource pool crossed its critical threshold for one kind. Producer fills at M17. Additive-only per M4 DR-002.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "schema_version": { "const": "prototype-recorder-event.v0.1" },
    "category": { "const": "resource" },
    "event_type": { "const": "critical" },
    "actor_id": { "type": ["integer", "null"] },
    "tick": { "type": "integer", "minimum": 0 },
    "payload": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "actor_id": { "type": "integer" },
        "kind": { "type": "string", "enum": ["blood", "oil", "power", "caloric", "bio_fluid", "oxygen_supply", "battery_charge", "internal_shock", "stamina"] },
        "threshold_pct": { "type": "number" },
        "current": { "type": "number" }
      },
      "required": ["actor_id", "kind", "threshold_pct", "current"]
    }
  },
  "required": ["schema_version", "category", "event_type", "tick", "payload"]
}
