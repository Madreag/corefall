# cf-e2e — AGENTS.md

## Owns
- (M0 stub) Will own the scripted end-to-end runner built on `cf-control`/`cfctl`. Drives milestone E2E commands (`--scenario`, `--script`, `--expect`, `--write-run-bundle`, `--ui-scale`, `--high-contrast`, `--verify-focus`, `--save-load-roundtrip`, `--verify-checksums`).

## Source Trail
- spec/prototype-roadmap §CLI Reference.
- spec/native-implementation-backlog M1.5 onward.
