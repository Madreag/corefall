# M5 — Deep Damage Event Surface Lock

## Status

`active`

## Intent

**M5 is the event-surface lock milestone** — the canonical schema for every event family that the damage / hazard / affliction / armor / internal / fluid / origin / atmospherics / shield / environment / thermal kernels will emit from M13 onward.

Per M4's locked v0.1 envelope: schemas declared here are **additive-only** for the rest of the project. New event types can be ADDED without bumping the envelope; existing event types cannot CHANGE shape.

M5 exists separately from M9 (Reactor Defense Scenario) because they have different implementer audiences:
- **M9** implementer ships a playable scenario
- **M5** implementer locks event schemas (no scenario work)

Both close BP3 closure gate; they're sister milestones.

M5 promise: **"every damage event from M9 forward emits the structured event family — no schema bump cascades when producers ladder up at M13/M14/M15/M16/M17/M19/M20."**

## What M5 does

M5 does NOT ship producer code. It ships:

1. Event schema JSON files at `game/crates/cf-replay/schemas/event/<family>_<type>.json`
2. Locked v0.1 envelope conformance (all schemas validate against the M4 locked envelope)
3. `cf-mod validate` rules for each event family
4. Cross-references to which milestone fills the producer for each event family

## Event families locked at M5

| Family | Schema locked at M5 | Producer ladders up at |
|---|---|---|
| `armor.*` | v0.1 (this milestone) | M13 (chassis 3-layer armor) + M14 (full collision) |
| `internal.*` | v0.1 | M14 (ray traversal) + M17 (per-origin organ/circuit graphs) |
| `concussion.*` | v0.1 | M17 (origin reaction matrix) |
| `internal_shock.*` (robot equivalent) | v0.1 | M17 |
| `fluid.*` | v0.1 | M13 (chassis fluid system) + M14 (collision puncture) |
| `origin.*` | v0.1 | M17 (origin reaction matrix) |
| `hazard.*` | v0.1 | M16 (hazard package) |
| `affliction.*` | v0.1 | M16 (22-affliction taxonomy) |
| `atmos.*` | v0.1 | M19 (Stationeers-grade atmospherics) |
| `shield.*` | v0.1 | M13+ (chassis shields) + M25+ (base shields) |
| `environment.*` | v0.1 | M20 (EnvironmentSignal aggregator) |
| `thermal.*` | v0.1 | M16 + M19 (heat exchange + material kernel) |
| `combat.projectile_hit_mo` (expanded payload) | v0.1 | M13 + M14 |

## Detailed event schemas (per family)

### armor.* family (M13+ chassis fills)

War Thunder-style angled armor with deflection, spalling, ricochet, ERA detonation, multi-hit degradation.

**Events:**

- `armor.layer_hp_changed { actor_id, item_id, zone, layer: External|Internal|Core, from, to, cause, ap_factor }`
- `armor.layer_critical { item_id, zone, layer, hp }`
- `armor.layer_destroyed { item_id, zone, layer, breach_kind: punctured|shattered|melted|chemically_corroded }`
- `armor.all_layers_destroyed { item_id, zone }`
- `armor.chunked_off { item_id, zone, debris_record_id, impact_impulse, debris_kind, ground_position }`
- `armor.debris_spawned { record_id, kind, material, position, velocity, can_pickup }`
- `armor.repaired { item_id, zone, layer, restored_hp, repaired_by_actor_id }`
- `armor.angle_deflection_calculated { impact_angle, nominal_mm, effective_mm }`
- `armor.ricochet { impact_angle, ricochet_probability, was_ricocheted: bool, deflection_vector }`
- `armor.spalling { item_id, zone, layer, fragment_count, damage_per_fragment, cause_event_id }`
- `armor.penetration_ray_traversed { ray_origin, ray_direction, modules_hit: Vec<(module_id, damage)>, final_resting_point }`
- `armor.he_overpressure_wave { center, radius, overpressure_pa, modules_affected: Vec<module_id> }`
- `armor.heat_jet_penetrated { item_id, zone, layer, jet_depth_mm }`
- `armor.heat_jet_pre_detonated_by_era { item_id, zone, era_panel_id }`
- `armor.apfsds_penetrated { item_id, zone, rod_length_remaining_mm }`
- `armor.era_panel_detonated { item_id, zone, era_panel_id, defeated_round_id }`
- `armor.schurzen_pre_detonated { item_id, zone, schurzen_plate_id, defeated_round_id }`
- `armor.multi_hit_degradation { item_id, zone, layer, hits_received, hardness_remaining }`
- `armor.reactive_armor_consumed { item_id, zone, panel_id }`

**Per-ammo ricochet thresholds (locked):**

| Round tier | Ricochet threshold (deg) |
|---|---|
| `standard` | 60 |
| `armor_piercing` | 65 |
| `hardened_AP` | 70 |
| `discarding_sabot` | 75 |
| `explosive_warhead` | 50 |
| `kinetic_impact` | 70 |
| `HEAT` | 75 |
| `APFSDS` | 80 |

**Effective thickness formula (locked):** `effective_thickness_mm = nominal_thickness_mm / cos(impact_angle_rad)`

**Spalling formula (locked):** if damage_to_armor > armor.spalling_threshold, spawn 1-3 fragments at random angles within 30° forward cone, each carrying 0.2-0.5 of original damage.

**Armor types** (M13 fills producer):

| Type | Hardness | Spalling | AP Resistance | HE Resistance | Special |
|---|---|---|---|---|---|
| Cast Iron | 0.6 | High (3 frag) | Low | Medium | Cracks easily |
| RHA | 0.8 | Medium (2 frag) | Medium | Medium | Standard |
| Composite | 0.85 | Low (1 frag) | High | Low | Layered |
| Ceramic | 0.95 | None | Very High vs AP | Low vs HE | Multi-hit degradation |
| Reactive (ERA) | 0.6 | None | Very High vs AP | Medium | One-shot per panel |
| Spaced | 0.7 | Low | Medium | Very High vs HEAT | Air gap |
| Schurzen | 0.4 | None | Low | Medium vs HEAT | Pre-detonates HEAT |
| Composite-ceramic | 0.92 | None | Very High | Medium | Endgame |
| Sloped Cast Steel | 0.7 | Medium | Medium (angled) | Medium | Natural 30-45° |

### internal.* family (M14+ ray traversal fills)

Per-organ (humans/androids) + per-circuit (robots) damage routing.

**Events:**

- `internal.organ_damaged { actor_id, organ_id, organ_kind, from_hp, to_hp, cause, source_hit_event_id }`
- `internal.organ_destroyed { actor_id, organ_id, organ_kind, failure_cascade }`
- `internal.organ_failure_cascade { actor_id, organ_id, applied_afflictions, hp_drain_per_s }`
- `internal.circuit_damaged { actor_id, circuit_id, circuit_kind, from_hp, to_hp }`
- `internal.circuit_destroyed { actor_id, circuit_id, circuit_kind, failure_cascade }`
- `internal.circuit_failure_cascade { actor_id, circuit_id, applied_afflictions }`

**15-organ humanoid graph (locked names):**

`brain` / `eyes_left` / `eyes_right` / `ears_left` / `ears_right` / `heart` / `lungs_left` / `lungs_right` / `liver` / `kidneys_left` / `kidneys_right` / `spine` / `stomach` / `intestines` / `pancreas`

**12-circuit robot graph (locked names):**

`power_core` / `cpu` / `sensor_array` / `motor_controller_left_arm` / `motor_controller_right_arm` / `motor_controller_left_leg` / `motor_controller_right_leg` / `hydraulic_pump` / `coolant_pump` / `oil_reservoir` / `fuel_tank` / `comm_relay`

### concussion.* + internal_shock.* family (M17 origin model fills)

**Events:**

- `concussion.dose_changed { actor_id, from_dose, to_dose, source_event_id, origin_id }`
- `concussion.band_changed { actor_id, from_band: Clear|Mild|Moderate|Severe|KO_Imminent|KO, to_band, dose }`
- `concussion.ko_threshold_crossed { actor_id, ko_duration_s }`
- `concussion.recovered { actor_id, recovery_reason: 'time' | 'medikit' | 'environment' }`
- `internal_shock.dose_changed { actor_id, from_dose, to_dose, source_event_id }` (robots)
- `internal_shock.module_damaged { actor_id, module_id, damage_amount, hit_zone, source_hit_event_id }`

**5-band accumulator thresholds (locked):**

| Band | Dose threshold | HUD cue |
|---|---|---|
| Clear | 0-20 | none |
| Mild | 20-40 | edge vignette 10% |
| Moderate | 40-60 | vignette 30% + bloom |
| Severe | 60-80 | vignette 60% + sway |
| KO_Imminent | 80-99 | vignette 85% + tunnel |
| KO | 100 | full blackout 5-10s |

**Per-origin decay rates (locked):**

| Origin | Concussion decay | Internal-shock decay |
|---|---|---|
| Human | 5/s | n/a |
| Android | 5/s | n/a |
| Robot | 0/s (always 0 dose) | 2/s |
| Powered organic | 5/s | n/a |
| Heavy biomech | 4/s | n/a |

### fluid.* family (M13+ chassis fluid system fills)

**Events:**

- `fluid.leak_started { actor_id, fluid_kind: 'oil'|'coolant'|'fuel'|'electrolyte', source_module_id, leak_rate, position }`
- `fluid.leak_rate_changed { actor_id, fluid_kind, from_rate, to_rate, reason }`
- `fluid.reservoir_warning { actor_id, fluid_kind, level_pct }` (at 50%)
- `fluid.reservoir_critical { actor_id, fluid_kind, level_pct }` (at 20%)
- `fluid.reservoir_empty { actor_id, fluid_kind, cascade_effects }`
- `fluid.ignition { actor_id, fluid_kind, ignition_source_event_id }` (combustible only)
- `fluid.ground_splatter_spawned { fluid_kind, position, volume_l, terrain_hazard_kind }` (cosmetic; M16 hazard kernel consumes)
- `fluid.leak_stopped { actor_id, fluid_kind, reason: 'sealed'|'repaired'|'reservoir_empty' }`
- `fluid.refilled { actor_id, fluid_kind, amount, source_actor_id }`

**4 fluid reservoirs (locked):**

| Fluid | Source module | Empty cascade |
|---|---|---|
| `oil` | oil_reservoir | Joint seizure; movement -50%; eventual motor failure |
| `coolant` | coolant_pump | Heat buildup; overheating affliction stacks |
| `fuel` | fuel_tank | Mobility offline; chassis inert; ignition risk |
| `electrolyte` | power_core | Action costs rise; eventually inert |

### origin.* family (M17 origin model fills)

**Events:**

- `origin.shot_force_feedback { actor_id, parent_hit_event_id, impulse_vector, impulse_magnitude, origin_id, chassis_layer, feedback_kind: 'pain_jolt'|'servo_jolt'|'frame_ring', g_load_delta, concussion_dose_delta, internal_shock_module_id, internal_shock_damage, leak_channel, leak_rate, screen_kick_intensity }`
- `origin.g_load_dose_changed { actor_id, from_dose, to_dose, source: 'fall'|'high_g_maneuver'|'rapid_impact' }`
- `origin.helmet_breach { actor_id, helmet_item_id, breach_pos, oxygen_loss_rate }`
- `origin.oxygen_supply_changed { actor_id, from_s, to_s, source }`

### hazard.* family (M16 hazard package fills)

**Events:**

- `hazard.spawned { hazard_id, kind: fire|smoke|electric|wet|hot|cold|acid|radiation|toxic, position, intensity, source_event_id }`
- `hazard.spread { from_pos, to_pos, kind, intensity, rate }`
- `hazard.actor_contact { actor_id, hazard_id, kind, intensity }`
- `hazard.tick { hazard_id, tick }` (cosmetic; batched 10:1 ratio for determinism)
- `hazard.dissipated { hazard_id, reason: 'time'|'doused'|'spread_out' }`

**5 launch hazard kinds (locked):** `fire`, `smoke`, `electric`, `wet`, `hot_cold` (M16 extends to 8 with `acid`, `radiation_zone`, `toxic_atmosphere`).

### affliction.* family (M16 affliction layer fills)

**Events:**

- `affliction.applied { actor_id, kind, source_event_id, expected_duration_ticks, severity_0_1 }`
- `affliction.tick { actor_id, kind, hp_delta, tick }` (per-tick application; consolidated cosmetic)
- `affliction.cleared { actor_id, kind, reason: 'time'|'medikit'|'environment'|'death' }`
- `affliction.escalated { actor_id, kind, from_severity, to_severity }`

**22 affliction kinds (locked names; mechanics in M16):** `burning`, `wet`, `electrified`, `poisoned`, `hypoxic`, `combustible_atmosphere`, `breach_decomp`, `hyperthermic`, `hypothermic`, `radiation`, `concussed`, `deafened`, `bleeding`, `internal_shock`, `low_battery`, `coolant_leaking`, `oil_leaking`, `overheating`, `hunger`, `thirst`, `sleep_dep`, `sanity_low`.

### atmos.* family (M19 atmospherics kernel fills)

**Events:**

- `atmos.pressure_changed { atm_id, from_pa, to_pa, source }`
- `atmos.temperature_changed { atm_id, from_k, to_k, source }`
- `atmos.gas_released { atm_id, gas: 'O2'|'N2'|'CO2'|'volatiles'|'pollutant'|'H2'|'N2O'|'H2O'|'O3'|'He', moles, source, ignition_risk: 0.0..1.0 }`
- `atmos.breach_detected { atm_id, breach_size_m2, source, decompression_rate_pa_per_s }`
- `atmos.combustion_ignition { atm_id, reaction_id, energy_release_j, temperature_after_k }`
- `atmos.phase_transition { atm_id, gas, from_phase, to_phase, latent_heat_consumed_j }`
- `atmos.pipe_flow { from_pipe_id, to_pipe_id, gas, moles_per_s }`
- `atmos.pipe_freeze { pipe_id, temperature_k }`
- `atmos.pipe_rupture { pipe_id, breach_position, pressure_at_rupture_pa }`
- `atmos.electrolysis_started { electrolyzer_id, input_water_kg_per_s, output_o2_kg_per_s, output_h2_kg_per_s }`

### shield.* family (M13+ chassis + M25+ base fill)

**Events:**

- `shield.hit { actor_id, hp_before, hp_after, cause }`
- `shield.depleted { actor_id, cause }`
- `shield.regen_started { actor_id }`
- `shield.regen_completed { actor_id }`
- `shield.disrupted { actor_id, duration_s, cause }`

**ShieldState struct (locked):** `{ hp, max_hp, regen_rate_per_s, downtime_after_break_s, status: Up|Down|Regenerating|Disrupted }`

### environment.* family (M20 aggregator fills)

**Events:**

- `environment.signal_delta { actor_id, slice: 'atmospheric'|'gravitational'|'thermal'|'radiation'|'photic'|'em'|'weather'|'water'|'acoustic'|'day_night'|'comms', from, to, tick }`
- `environment.signal_aggregated { actor_id, tick, signal: EnvironmentSignal }` (full bundle; cosmetic-batchable)

### thermal.* family (M16+ material kernel fills)

**Events:**

- `thermal.signature_changed { actor_id, from_k, to_k }`
- `thermal.heat_exchanged { from_tile, to_tile, joules }` (per-tile heat flow; ONI-style)
- `thermal.material_phase_change { material_id, from_phase, to_phase, position, latent_heat_consumed_j }`

### combat.projectile_hit_mo expanded payload

Locked payload at M5; M13 + M14 fill producers:

```text
combat.projectile_hit_mo {
  // who shot what at whom
  shooter_id: ActorId,
  weapon_id: ItemId,
  projectile_id: ProjectileId,
  target_id: ActorId,
  hit_zone: BodyZone,
  // physical truth
  impact_point: Vec2,
  impact_normal: Vec2,
  impact_impulse: f32,
  impact_energy_j: f32,
  // armor characterization
  ap_factor: f32,
  ap_round_tier: 'standard'|'armor_piercing'|'hardened_AP'|'discarding_sabot'|'explosive_warhead'|'kinetic_impact'|'HEAT'|'APFSDS',
  material_at_impact: MaterialId,
  surface_kind: 'armor_external'|'armor_internal'|'armor_core'|'armor_chunked_breach'|'flesh'|'circuit'|'unarmored'|'terrain',
  armor_effective_hardness: f32,
  armor_absorbed_dmg: f32,
  passthrough_dmg: f32,
  // damage result
  damage_kind: 'kinetic'|'thermal'|'electric'|'chemical'|'radiation',
  hp_before: f32,
  hp_after: f32,
  damage_amount: f32,
  layer_struck: Option<ArmorLayer>,
  pierced_armor: bool,
  // internal damage routing
  organ_damaged_id: Option<OrganId>,
  circuit_damaged_id: Option<CircuitId>,
  // attribution
  parent_event_id: EventId,
  cosmetic: false,
}
```

### Sound clip variants per armor material + impact state

Per-material + per-impact-state combination (locked taxonomy):

| Material | Pristine hit | Cracked hit | Destroyed hit | Chunked-off | Pierce |
|---|---|---|---|---|---|
| `metal` | Sharp metallic ring | Dull clang | Muffled scrape | Heavy clang + falling debris | Crisp pierce |
| `ceramic` | Sharp crack | Wide crack pattern | Crumble | Shattering | Sharp shatter |
| `composite` | Thud | Hollow thud | Plastic crunch | Crack + fall | Plastic-pierce |
| `cloth` | Light swish | Tear | Rip | (chunked = ripped section drops) | Soft tear |
| `leather` | Creak | Crack | Shred | (chunked = fragment drops) | Squelch-tear |
| `hardened_plate` | Strong clang | Sharp ring | Heavy thunk | Massive falling impact | Sharp pierce + ring |
| `reactive_armor` | Sharp pop | Light explosion | Multiple pops | Detonation + scatter | Explosive pierce |

Internal hit sounds (locked):

| Internal hit | Sound |
|---|---|
| `flesh_punctured` | Wet squelch |
| `bone_cracked` | Snap |
| `organ_ruptured` | Wet rupture |
| `circuit_sparked` | Electric zap |
| `circuit_destroyed` | Buzz + smoke |
| `fluid_pierce` | Liquid splash |

All sound events emit `audio.event_requested` with `kind: material_state` + `surface_kind` + `damage_kind`. M13.x+ cf-audio consumes; M5 just locks the request shape.

## Acceptance criteria

```gherkin
Scenario: All event family schemas exist at v0.1
  Given M5 closure
  Then game/crates/cf-replay/schemas/event/ contains JSON files for all locked families
  And cf-mod validate game/crates/cf-replay/schemas/ exits 0
  And each schema declares schema_version="0.1" matching the M4 locked envelope

Scenario: Schemas accept producer events from later milestones additively
  Given M13 ships chassis.armor_layer_destroyed
  When cf-replay envelope validates the event
  Then it conforms to armor.layer_destroyed.json (just with bound_zone added)
  And no envelope bump required (additive payload extension)

Scenario: cf-mod validate covers all M5 families
  Given content/* references events from any M5 family
  When cargo run -p cf-mod -- validate content/ runs
  Then exit 0 if events conform to schemas
  And exit non-zero with structured error if event shape drifts

Scenario: M4 locks the envelope at v0.1 (cross-reference)
  Given M4's envelope schema
  Then M5's per-event schemas all conform to it
  And bumping schema_version requires migration tooling (deferred to BP6+ per M4)
```

## Dependencies

- **M4 (event recorder; must be done OR concurrent)** — M5's schemas conform to M4's locked envelope

## Closure procedure

Reference bundle (any M9-scenario bundle that produces M5-conformant events). All schemas exist; all cf-mod validation passes. Move M5.md → done/.

## Cross-DR

DR-002 (locked envelope), DR-024.

## Implementer notes

M5 is purely a **declarative milestone**. The implementer:

1. Reads M5.md (this file)
2. Creates one JSON schema file per event family member at `game/crates/cf-replay/schemas/event/<family>_<type>.json`
3. Runs `cargo run -p cf-mod -- validate game/crates/cf-replay/schemas/` to verify conformance
4. Does NOT touch any producer code (M13/M14/M15/M16/M17/M19/M20 implementers do that)

Each schema follows this skeleton (per M4's envelope):

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "armor.layer_destroyed",
  "type": "object",
  "properties": {
    "schema_version": { "const": "0.1" },
    "category": { "const": "armor" },
    "event_type": { "const": "layer_destroyed" },
    "actor_id": { "type": "integer" },
    "tick": { "type": "integer" },
    "payload": {
      "type": "object",
      "properties": {
        "item_id": { "type": "integer" },
        "zone": { "type": "string", "enum": ["head", "torso", "arm_left", "arm_right", "forearm_left", "forearm_right", "hand_left", "hand_right", "leg_left", "leg_right", "shin_left", "shin_right", "foot_left", "foot_right", "backpack"] },
        "layer": { "type": "string", "enum": ["External", "Internal", "Core"] },
        "breach_kind": { "type": "string", "enum": ["punctured", "shattered", "melted", "chemically_corroded"] }
      },
      "required": ["item_id", "zone", "layer", "breach_kind"]
    }
  },
  "required": ["schema_version", "category", "event_type", "tick", "payload"]
}
```

Counting: M5 registers ~60-80 event schemas across 13 families (armor, internal, concussion, internal_shock, fluid, origin, hazard, affliction, atmos, shield, environment, thermal + combat.projectile_hit_mo expanded payload). One JSON file per event type.
