
## Status

`active`

## Intent

**M5.9.5 is the BP5 micro-fun-slice** — a 60-90s `micro_pressure_hold` scenario that proves M5.9 atmospherics + M5.10 environment aggregator work as a playable mission. Per BP5 fun-proof gate.

M5.9.5 promise: **"hold pressure in a breached habitat while the atmosphere fights you — every aperture, every gas, every breath matters."**

## Player-facing behavior

Scenario: micro_pressure_hold (60-90s):
- Habitat with 4 connected rooms; one wall has a slowly-growing breach
- Player must: seal breach (with foam/concrete tool) OR pump O2 from reserve tank
- Atmosphere drains via aperture per PV=nRT
- Toxic gas creeps in from adjacent compartment (combustible_atmosphere)
- Player can vent toxic gas via airlock (but loses pressure faster)
- Win: pressure > 80kPa at 60s mark. Lose: pressure < 30kPa OR oxygen < 15s.

### Content roster at M5.9.5

| Content | Roster |
|---|---|
| **Base objects** (toward 60+) | 12 launch base objects (airlock, pump, vent, regulator, valve, filter, condenser, oxygen tank, fuel tank, repair drone, sensor, alarm) |
| **SFX** (toward 400+) | 140 cumulative |
| **Missions** (toward 30+) | 6 missions cumulative |

## Acceptance criteria

```gherkin
Scenario: Pressure hold survives 60s
  Given micro_pressure_hold scenario at start
  When player seals breach with foam gun within 30s
  Then pressure stabilizes; mission won

Scenario: Atmosphere drains via aperture
  Given breach with aperture_area=0.5m²
  Then atmos.pressure_changed fires per tick
  And pressure drops per PV=nRT

Scenario: Toxic gas combustible cascade (loss)
  Given toxic gas + spark
  Then combustion_ignition fires; explosion damages remaining seals
```

## Dependencies

- M5.9 + M5.10 must close.

## Closure procedure

Reference bundle + 4 sweep rows (win/loss/abort/120hz_determinism); all PASS.
