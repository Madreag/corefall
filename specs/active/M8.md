# M8 — UX + Camera + Debug + Localization + Accessibility

## Status

`active`

## Intent

**M8** is the **third (final) bridge slice** between M7 (AI + mission director) and M9 (reactor defense). It closes the **camera + UX + debug + localization + accessibility-extras + basic photo/replay/killcam** gap. After M8, the M6-M8 bridge ladder is complete and the game has production-quality UX polish before M9 ships reactor defense.

M8's player-facing promise: **"the production-quality UX feels polished and accessible to everyone."**

## Player-facing behavior

### Camera + game feel

- **Smooth follow** with lerp + lookahead + deadzone (200ms ease)
- **Hit-stop** on impactful hits (50-200ms frame freeze; configurable; cosmetic per M4)
- **Scope zoom** (sniper ADS — 30° FOV; reticle precision boost ×0.3 bloom)
- **Free-look** (RMB toggle; camera follows cursor within max distance)
- **Camera punch** from M1 extended with per-weapon recoil profile
- **Slow-motion accessibility** (`game_speed_assist`: Off / Slowdown75 / Slowdown25 / FullPause)

### UX widgets (10+ new HUD widgets)

| Widget | Position | Purpose |
|---|---|---|
| **8-slot hotbar** | bottom row | Icons + ammo count + suppressor/bipod state badges; active slot highlighted |
| **Mini-map / radar** | top-right (256×256) | Player + enemies + waypoints + zoom adjustable |
| **Compass** | top-center (360°) | Cardinal letters + waypoint bearings |
| **Damage direction indicator** | screen edge | Red arrow pointing toward damage source (fades after 1s; reduce_motion: instant on/off) |
| **Squad strip** | top-right next to ammo | 1-4 members with role + HP + alert badge |
| **Stamina bar** | under stance pip | Color: cyan >50%, yellow <50%, red <10% |
| **Stealth meter** | near reticle | Eye icon with detection % |
| **Grenade arc preview** | translucent path | Predicted trajectory; updates per aim |
| **Action prompts** | contextual | "PRESS E TO PICK UP" / "PRESS X TO CROUCH" etc. |
| **Weapon swap overlay** | center | Large icon during 300ms transition |
| **Scope reticle** | center | Sniper ADS overlay (large + precision crosshair) |
| **Phase strip** | top | Current mission phase 1-4 + countdown |
| **Branching banner** | center | "KILL or SNEAK" option for branching objectives |
| **Cover state pip** | near reticle | "COVER: None/Partial/Full" |
| **Lean angle indicator** | near reticle | -45° to +45° |

### Settings menu tree (6 tabs)

Per `settings-menu-architecture-direction` (M33+ shell UI ladders up):
- **Graphics**: resolution / VSync / quality preset / render scale (placeholder for M33+ shell)
- **Audio**: master / SFX / music / voice / ambient (M37+ fills voice)
- **Controls**: key remapping (per M11 18-action surface; M6 expands to ~50 actions)
- **Accessibility**: text_scale, contrast, captions, color_cue_mode, hold_to_confirm, hold_threshold_ms, reduce_motion/shake/flash, ui_density, aim_assist, mini_map_zoom, scope_zoom_fov, damage_numbers, killcam_enabled, hit_stop_enabled
- **Gameplay**: difficulty (cakewalk/tough_crowd/veteran) + difficulty modifiers (permadeath, no_respawn, fog_of_war_on, limited_ammo, time_limit, no_minimap, hardcore_mode, friendly_fire_on)
- **Language**: English baseline (Tier-A 11 languages reserved for T-ACC-PLUS BP9+)

### Debug overlays (7 toggleable; F1-F7 in dev builds)

**`cf-debug` NEW crate** with overlay registry:

| Overlay | Hotkey | Shows |
|---|---|---|
| AI state | F1 | Per-AI sight cone + hearing radius + memory grid + state label |
| Pathfinding | F2 | Current path + alternates + cost (forward-compat M22) |
| Collision shapes | F3 | AABB outlines for actors + items + projectiles |
| Material affordance | F4 | Cursor tooltip: material + integrity + 9 affordance flags |
| Physics impulses | F5 | Force vectors for actors with recent impulses |
| Sound propagation | F6 | Loudness ripples from sources + occlusion visualization |
| Squad command | F7 | Waypoint pins + doctrine labels per squad |

Production builds gate behind `Settings.debug.enabled=true`.

### Localization (cf-localization NEW crate)

Keyed strings table per DR-046 + DR-051:
- Loaded from `content/localization/<lang>.json`
- ~500 launch keys covering: HUD labels, banners, captions, settings labels, tooltips, weapon/tool names, command labels, achievement names, error messages
- **English baseline at M8**
- **ICU MessageFormat compliance** forward-compat
- Tier-A 11 languages reserved for T-ACC-PLUS BP9+
- `cfctl observe.localization.current_language` returns active language

### Accessibility — extended floor

Extends M11's ACC-A floor with M8 additions:
- **Color blind mode variants** (Default / ColorblindSafe / Protanopia / Deuteranopia / Tritanopia / Monochrome-test) — per-channel color shift; pixel-art friendly texture variants
- **Aim assist** (3 modes: off / steady_aim / auto_aim_with_damage_penalty)
- **Speedrun mode** with persistent timer + segment markers (forward-compat for leaderboard M36+)
- **Difficulty modifiers** (granular per scenario)
- **Damage numbers** (toggleable cosmetic; floating text on hit)
- **Custom HUD layouts** (basic drag-drop; save preset; M33+ fills full editor)

### Pie menu — context-sensitive radial actions (M1 reserves; M8 fills baseline)

Per CCCP `Controller.cpp:280` + `AHuman.cpp:2962-3020`. M1 reserved the **CONTROLS CAPTURED HUD label** surface; M8 fills the **full ring UI**.

- Press `T` (default; remappable) → radial 8-slice menu opens; sim transitions to slowdown75 OR full pause (per Settings)
- 8 slices per default (M8 baseline; M13+ extends with chassis-specific slices):
  1. **Pickup** (E action)
  2. **Drop** (Q action)
  3. **Switch Weapon** (1-8 cycle)
  4. **Throw Grenade** (T action)
  5. **Melee Bash** (M action)
  6. **Deploy Bipod** (B action, when crouched/prone with bipod-compatible weapon)
  7. **Signal Squad** (signal_friendly + mark_waypoint)
  8. **Use Medkit** (heal self; M13+ chassis extends with chassis-specific repair)
- Per-slice **reason labels** for disabled actions (per CCCP `:2962-3020`):
  - "No Arm" — pickup disabled when arm destroyed
  - "Not Holding Anything" — drop disabled when slot empty
  - "Needs Digger" — context tool refusal
  - "Out of Range" — context proximity refusal
  - "No Grenade Equipped" — grenade throw disabled when no grenade in inventory
  - "Standing — Requires Crouch/Prone" — bipod disabled when stance prevents
- Reachable via keyboard (Tab through slices + Enter) + controller (D-pad rotation + South button)
- Visual: ASCII glyph + text label per slice (DR-012 monochrome readable)
- Events: `ux.pie_menu_opened`, `ux.pie_menu_slice_chosen`, `ux.pie_menu_closed`, `ux.pie_menu_slice_rejected { slice, reason }`

M13+ extends with chassis-specific slices (eject pilot / repair chassis / overclock / cockpit camera anchor / ability slots / brain_hop).

### Photo mode + Replay scrubber + Killcam (basic surfaces; M33+/M36+ full)

**Photo mode** (`cf-photo` NEW stub):
- Press F12 → enter photo mode (sim pauses)
- Free camera (pan/zoom/rotate/FOV)
- Apply filters (sepia / B&W / color grading / cyberpunk neon — 4 launch filters)
- Export to PNG
- Forward-compat for M33 codex

**Replay scrubber** (`cf-replay-scrub` NEW stub):
- Mini-replay timeline at bottom of HUD
- Scrub through last 30s of action (slow-motion review)
- Bookmark interesting moments (`act.replay.bookmark`)
- Forward-compat for M33+ replay browser

**Killcam** (`cf-killcam` NEW stub):
- On player death, 3-second slow-motion replay from killing enemy's perspective
- Toggleable per accessibility (some prefer it off)
- Local at M8; network forward-compat for M36+ multiplayer

**Slow-mo kill cam** (per DR-055):
- On final blow of mission boss, brief 1.5s cinematic camera angle
- Toggleable

## Crates / modules touched

| Crate | Status | What changes |
|---|---|---|
| `cf-camera` | NEW | Camera mode + smooth lerp + hit-stop + scope + free-look |
| `cf-debug` | NEW | Debug overlay registry + 7 overlays |
| `cf-localization` | NEW | Keyed strings + ICU MessageFormat |
| `cf-photo` | NEW (stub) | Photo mode + filters + export |
| `cf-replay-scrub` | NEW (stub) | Mini-timeline + scrubbing + bookmark |
| `cf-killcam` | NEW (stub) | Killcam state machine + 3-second replay |
| `cf-ui` | MODIFY (deep) | 10+ new HUD widgets + settings menu tree |
| `cf-control` | MODIFY | Settings API extended + photo + replay scrub + killcam controls |
| `cf-replay` | MODIFY | Event families: camera.hit_stop, photo_mode.*, replay.scrub_*, replay.bookmark_*, killcam.*, ux.hud_layout_changed, ux.preset_saved |

## Files

- `game/crates/cf-camera/src/{lib,follow,hit_stop,scope,free_look}.rs` (NEW)
- `game/crates/cf-debug/src/{lib,ai_overlay,pathfinding_overlay,collision_overlay,material_overlay,physics_overlay,sound_overlay,squad_overlay}.rs` (NEW)
- `game/crates/cf-localization/src/{lib,keyed_strings,icu_format}.rs` (NEW)
- `game/crates/cf-photo/src/{lib,filters,export}.rs` (NEW)
- `game/crates/cf-replay-scrub/src/{lib,timeline,bookmark}.rs` (NEW)
- `game/crates/cf-killcam/src/{lib,recorder,playback}.rs` (NEW)
- `game/crates/cf-ui/src/{hotbar,minimap,compass,damage_direction,squad_strip,stamina_bar,stealth_meter,grenade_arc,action_prompt,weapon_swap_overlay,scope_reticle,phase_strip,branching_banner,cover_pip,lean_pip,settings_menu}.rs` (NEW)
- `game/content/localization/en.json` (NEW: ~500 keys)

## Acceptance criteria

### Camera + game feel

```gherkin
Scenario: Smooth follow with lookahead
  Given player sprint right
  Then camera offset shifts forward (lookahead)
  And lerps to position over 200ms

Scenario: Hit-stop on impact
  Given melee hit OR AP round hit
  When hit lands
  Then camera.hit_stop fires for 100ms
  And per-frame time pauses (rendering continues)
  When Settings.hit_stop_enabled=false: no hit-stop

Scenario: Scope zoom (sniper ADS)
  Given sniper weapon equipped
  When act.player.scope_zoom fires
  Then camera.mode=Scope, FOV=30°
  And HUD shows scope reticle overlay

Scenario: Free-look (RMB hold)
  Given any actor
  When act.player.free_look_toggle fires
  Then camera.mode=FreeLook
  And camera follows mouse cursor
  Release: returns to Follow

Scenario: Slow-motion (game_speed_assist)
  Given Settings.game_speed_assist=Slowdown75
  Then sim runs at 75% speed
  And all events deterministic (replay-compatible)
  Slowdown25: 25% speed
  FullPause: 0% (menu only)
```

### HUD widgets

```gherkin
Scenario: 8-slot hotbar
  Given 8 items equipped
  When player presses 1-8 keys
  Then active slot cycles + HUD highlights

Scenario: Mini-map widget
  Given player at position (X, Y)
  Then HUD shows mini-map (256×256) top-right
  And player icon centered + enemies visible + waypoints marked

Scenario: Compass widget
  Given player facing direction X
  Then HUD shows compass top-center with cardinal letters
  And waypoint bearings shown

Scenario: Damage direction indicator
  Given hit from direction θ
  Then HUD shows red arrow on screen edge pointing toward θ
  And arrow fades after 1s
  When reduce_motion=true: no fade animation

Scenario: Stealth meter eye icon
  Given perception_overlay_enabled=true
  Then HUD shows eye icon with detection %
  When detection > 50%: caption "Spotted" surfaces

Scenario: Grenade arc preview
  Given frag grenade + player aiming
  Then translucent arc shown from hand to predicted impact

Scenario: Action prompt
  Given player near dropped item
  Then HUD shows "PRESS E TO PICK UP"

Scenario: Phase strip + branching banner
  Given mission running
  Then phase strip top of HUD (1=Setup, 2=Buildup, 3=Climax, 4=Debrief)
  Given branching trigger
  Then banner appears with options ("KILL or SNEAK")
```

### Debug overlays

```gherkin
Scenario: AI state overlay shows perception radii
  Given F1 pressed (AI state)
  Then each AI's sight cone + hearing radius + memory grid visible
  And state label floats above each enemy

Scenario: Pathfinding overlay
  Given F2 pressed
  Then current path drawn for each AI

Scenario: Collision shapes overlay
  Given F3 pressed
  Then AABB outlines drawn for actors + items + projectiles

Scenario: Material affordance overlay
  Given F4 pressed + cursor on terrain
  Then tooltip: material name + hardness + integrity_band + 9 affordance flags

Scenario: Physics impulses overlay
  Given F5 pressed
  Then force vectors drawn for actors with recent impulses

Scenario: Sound propagation overlay
  Given F6 pressed
  Then loudness ripples from sound sources + occlusion visualization

Scenario: Squad command overlay
  Given F7 pressed
  Then waypoint pins + doctrine labels visible
```

### Localization

```gherkin
Scenario: All HUD text uses keyed strings
  Given localization table loaded
  Then no English literals baked into HUD widgets
  And all labels resolve via key lookup
  And ~500 keys cover HUD/banners/captions/settings/tooltips

Scenario: Language switching
  Given Settings.language="en"
  Then HUD renders English
  When Settings.language="es" (future):
    Then HUD switches without restart

Scenario: ICU MessageFormat for pluralization
  Given event "1 enemy spotted" or "3 enemies spotted"
  Then plural form resolves via ICU MessageFormat
```

### Accessibility extras

```gherkin
Scenario: Color blind mode applies palette swap
  Given Settings.color_cue_mode=ColorblindSafe + Protanopia
  Then critical states use yellow + blue (not red + green)
  And monochrome screenshot reads correctly

Scenario: Aim assist
  Given Settings.aim_assist=steady_aim
  Then reticle wobble × 0.5
  Settings.aim_assist=auto_aim:
    Then reticle snaps slightly toward target + damage × 0.85

Scenario: Speedrun mode
  Given Settings.mode=speedrun
  Then HUD shows persistent timer + segment markers
  And mission resolves immediately on objectives

Scenario: Damage numbers
  Given Settings.damage_numbers=true
  When player hits enemy
  Then HUD shows floating "+15" near hit point
  And color = red (kinetic) / orange (thermal) / cyan (electric)
  And the number fades after 1s

Scenario: Custom HUD layout (basic)
  Given player in HUD layout mode
  When player drags widget to position
  Then ux.hud_layout_changed fires
  Save preset: ux.preset_saved fires
```

### Photo mode + Replay scrubber + Killcam

```gherkin
Scenario: Photo mode export
  Given player presses F12
  Then photo_mode.entered fires + sim pauses
  And camera free + 4 filters available
  When player captures: photo_mode.shot_taken fires
  And PNG exported

Scenario: Replay scrub
  Given replay scrub button pressed
  Then mini-timeline opens (last 30s)
  When player scrubs back: timeline shows past actions
  When player bookmarks: replay.bookmark_added fires

Scenario: Killcam on player death
  Given player dies from enemy fire
  Then killcam.played fires
  And 3-second slow-motion shown from enemy perspective
  When Settings.killcam_enabled=false: skipped

Scenario: Slow-mo kill cam on boss final blow
  Given boss HP=0
  Then slow_mo.kill_cam_triggered fires
  And 1.5s cinematic camera + dramatic angle
  When Settings.cinematic_kills=false: skipped
```

## Out of scope (defers to later milestones)

- **Map editor (mini)** → M33 mod workbench (per CCCP gem; deferred to give M33 the full editor surface)
- **Lua script editor** → M33 mod workbench
- **Full photo mode UI** (filter palette, color grading curves) → M33 codex
- **Full replay browser with scrubbable timeline** → M33 replay browser
- **Custom HUD layouts full editor** → M33 shell UI
- **Spectator mode camera + transitions** → M36+ multiplayer
- **Tier-A 11 languages** → T-ACC-PLUS BP9+
- **Cosmetic locker** → M33+ shell UI / M25+ campaign rewards
- **Streamer mode / Twitch integration** → M36+ post-launch

## Dependencies

- **M6 + M7 (must close)**: M8 consumes all UX surface output from prior slices
- **M11 in flight**: M8 inherits ACC-A floor + extends with color blind / aim assist / damage numbers
- **M4 in flight**: M8 produces 20+ new event types

## Closure procedure (M8 → CLOSED)

1. Reference bundle: `prototype_runs/native/m8_<UTC>_<hash>/`.
2. Self-play sweep rows: `m2.2c_camera_modes`, `m2.2c_hit_stop`, `m2.2c_scope_zoom`, `m2.2c_free_look`, `m2.2c_minimap`, `m2.2c_compass`, `m2.2c_damage_direction`, `m2.2c_squad_strip`, `m2.2c_stamina_bar`, `m2.2c_stealth_meter`, `m2.2c_grenade_arc`, `m2.2c_action_prompts`, `m2.2c_phase_strip`, `m2.2c_branching_banner`, `m2.2c_settings_menu`, `m2.2c_debug_7_overlays`, `m2.2c_localization_en`, `m2.2c_colorblind_4_modes`, `m2.2c_aim_assist`, `m2.2c_speedrun_mode`, `m2.2c_difficulty_modifiers`, `m2.2c_damage_numbers`, `m2.2c_custom_hud_basic`, `m2.2c_photo_mode_basic`, `m2.2c_replay_scrub_basic`, `m2.2c_killcam`, `m2.2c_slow_mo_kill_cam`, `m2.2c_5min_endurance`, `m2.2c_120hz_determinism`. All PASS.
3. Move `specs/active/M8.md` → `specs/done/M8.md`.
4. Trigger M9 audit + closure (M6 + M7 + M8 → M3 → M9 dependency chain complete).

---

## Feature routing — where the M6-M8 bridge mega-features landed

The original mega-bridge attempted 80+ features. The split into M6/M7/M8 kept the **core bridge** (actor controller + AI + UX) and distributed the **beyond-most-games** features to their proper later milestones.

### M6-M8 core (kept in M6/M7/M8 — must ship before M9)
- M6: 36 actor actions / 6 weapons / 4 grenades / 4 melee / 7 tools / 8-slot inventory / sound propagation / 1 friendly bot + 4 squad commands / stealth kill / knife throw / rope-pipe-ladder climbing / damage falloff / tool degradation
- M7: 5 AI archetypes / cover seeking + suppression + retreat + squad comm + patrol / personality traits (basic, affects AI) / mood/stress (basic) / squad doctrine / faction (basic 3-faction) / mission director v0.5 / multi-objective + branching + 4 phases / reinforcement waves / mini-boss
- M8: camera (smooth follow + hit-stop + scope + free-look) / 10+ HUD widgets / settings menu tree / 7 debug overlays / localization (keyed strings, English) / color blind variants / aim assist / speedrun mode / difficulty modifiers / damage numbers / custom HUD (basic) / photo mode (basic) / replay scrubber (basic) / killcam / slow-mo kill cam

### Moved to M13 (chassis closure)
- **Brain hopping** — Cortex-family brain identity + multi-actor switch (per CCCP `Activity.cpp`)
- **Crab / quadruped actor variant** — non-humanoid chassis form
- **Pilot-inside-chassis dual silhouette** — M11 already reserves; M13 fills
- **Cockpit camera anchor** — first-person mech view
- **Chassis weight class layouts (Light/Medium/Heavy/Drone)** — M11 already reserves
- **Boarding / disembarking transitions** — M11 reserves; M13 fills
- **Ejecting + Salvaging modes** — M11 reserves; M13 fills
- **Time stop / time slow ability** — M13+ chassis ability slot
- **Weapon modifier slots (combinatorial Noita-style)** — M13+ chassis ability slot
- **Drone allies + autonomous workers** — drone = small chassis
- **Hit reactions per body part** — M13 chassis adds per-zone reactions

### Moved to M14 (full collision + impulse routing)
- **Ragdoll on death** — DR-033 closure
- **Limb detachment from joint impulse** — CCCP `MOSRotating::DetachAttachablesFromImpulse`
- **Gib spawning with debris** — CCCP `MOSRotating::CreateGibsWhenGibbing`
- **Per-organ internal damage routing** — M9 reserves cf-personality; M14 fills

### Moved to M15 (active-material kernel)
- **Material reactions (acid + iron → rust, water + fire → steam, oil ignition)** — Noita-grade
- **Material state transitions (water → steam → cloud)** — full phase change
- **GPU-accelerated falling-sand simulation** — Noita scaling
- **Material registry with 50+ materials** — full Powder Toy + ONI / Stationeers depth
- **Complex chemistry reactions (alchemy table)** — M15 active-material kernel
- **Flask system (liquid containers)** — uses M15 material kernel

### Moved to M16 (hazard package + afflictions)
- **All 18 affliction kinds with full mechanics** — M9 reserves event surface; M16 fills producers
- **Hazard tile full simulation (fire spread, smoke pressure-driven, electric arcs, wet/oil pooling, hot/cold conduction)** — M16
- **Anomaly hazards (gravity / electric / time / chemical / bloodsucker / psy_storm)** — STALKER-style
- **Artifact loot (20+ artifacts with passive bonuses)** — STALKER-style
- **Swimming + underwater combat (water material)** — M16 water material

### Moved to M17 (origin reaction model)
- **G-Force vision blackout curves per origin** — M9 reserves; M17 fills
- **Caloric_energy + battery_charge + power resources** — M17 fills
- **Helmet + oxygen tank vacuum scenarios** — M9 reserves; M17 fills
- **Concussion / internal_shock dose curves per origin** — M9 reserves; M17 fills
- **Full personality + backstory + faction relationships (DF/Rimworld depth)** — M17 origin retention

### Moved to M19 (atmospherics kernel)
- **Real PV=nRT gas chemistry + combustion stoichiometry** — Stationeers-grade
- **Pipe networks as first-class atmospheres** — Stationeers-grade
- **Phase change with latent heat** — full simulation

### Moved to M20 (EnvironmentSignal aggregator)
- **One bundle per actor per tick** — aggregation layer
- **All consumers read from one aggregator** — M20

### Moved to M22 (AI pathfinding + collision avoidance)
- **Hierarchical pathfinding (H-PA*)** — M22 fills
- **Dynamic terrain re-pathing** — M22 fills
- **Per-team path costs** — M22 fills
- **Multi-actor collision avoidance** — M22 fills

### Moved to M25 (campaign + base + commander)
- **Mid-mission buy menu + delivery craft** — CCCP signature; per `BuyMenuGUI` + `RocketAI.lua`
- **Gold/credit economy + material drops** — per CCCP `Actor.cpp:829-848`
- **Bunker / base building blocks** — per CCCP `Data/Base.rte/Scenes/Objects/Bunkers`
- **Door buy system** — per CCCP `BuyDoor.lua`
- **Drop pod + scuttle mechanics** — M25+ logistics
- **Full faction system (territory + war + quartermaster vendors + diplomacy)** — M25 campaign
- **Veteran actors + named bots + scars** — M25 retention
- **Storyteller / incident director (Cassandra/Phoebe/Randy)** — M25 mission director
- **Pet / companion animals** — M25 retention
- **Anomaly hazards full mechanics** — M25+ campaign (M16 has surface)
- **Treasure maps + voyages** — Sea of Thieves
- **NPC dialog system** — M25 campaign narrative
- **Hostage / captives mechanics** — M25 campaign
- **Investigation events** — M25 campaign
- **Crime scene exploration** — M25 campaign
- **Hidden lore collectibles** — M25+ codex
- **Stratagem call-ins (8 stratagems)** — Helldivers; M25+ mission director
- **Stratagem cool-down + friendly-fire warnings** — M25+
- **Hot-drop deployment** — M25+ logistics
- **Loot rarity tiers (Common / Magic / Rare / Legendary / Unique)** — Diablo; M25+ retention
- **Item affixes (30+)** — Diablo; M25+
- **Set bonuses** — Diablo; M25+
- **XP + level + perk-point system** — M25+ retention
- **Achievements + collectibles (30+ launch)** — M25+ retention
- **Inventory grid Tetris** — Stationeers-grade UI; M33 loadout workbench
- **Manufacturing recipes** — Factorio-style; M25+ base economy
- **Cooking + food + plant growing** — Stationeers + Rimworld; M25+ base economy
- **Perk + curse system (20+ perks / 10+ curses)** — Noita; M25+ retention
- **Mood breaks (berserker / shock / depression)** — Rimworld; M25+ campaign
- **Schedules (sleep / work / recreation)** — Rimworld; M25+ campaign
- **Boss patterns full (multi-phase)** — M25+ campaign (M7 has mini-boss surface)

### Moved to M31 (weather + day/night kernel)
- **Day/night cycle full simulation** — per DR-040
- **Weather state machine (clear / rain / storm / dust / fog / snow / acid_rain)** — per DR-040
- **Dynamic weather events (lightning, dust storms)** — M31

### Moved to M33 (cf-tools-editor + mod workbench)
- **Full in-game scenario editor with drag-drop** — M33 mod workbench
- **Lua script editor with syntax highlighting** — M33
- **Mod manifest + workshop integration** — M33
- **Full photo mode UI (filters, color grading curves)** — M33 codex
- **Full replay browser with scrubbable timeline** — M33 replay browser
- **Custom HUD layout drag-drop full editor** — M33 shell UI
- **Spectator mode camera + transitions** — M33 dev tools
- **Replay sharing (workshop + filesystem)** — M33+
- **IC10 programmable chips + logic gates + electronics graph** — Stationeers; M33 mod workbench
- **Wire/pipe network construction** — Stationeers; M25+ base building
- **Sensor + analyzer system** — Stationeers; M25+ base building
- **Lua scripting hooks for items** — CCCP `Medikit.lua`; M33 mod workbench
- **8 modular tutorial labs** — M33+ campaign onboarding (M7 has mission director surface)

### Moved to M36 (multiplayer)
- **2-player co-op session with shared mission** — M36 fills
- **Spectator mode network** — M36 fills
- **Networked killcam from other perspective** — M36+ fills (M8 ships local)
- **Friend lobby + matchmaking** — M36
- **Anti-cheat layer** — M36
- **Vehicle driving (jeep / ATV / motorcycle)** — M25+ logistics OR M36+ multiplayer races

### Moved to M37 (voice + comms)
- **Voice / radio chatter pre-cursor** — M37 fills full voice
- **Subtitle system for radio chatter** — M37 fills

### Moved to M41 (squad command + AI commander)
- **Multi-squad command (4+ squads)** — M41 fills
- **Squad role differentiation (rifleman + medic + sniper + heavy)** — M41
- **AI commander persona (named enemies, persistent rivalries)** — M41+ multi-squad

### Moved to M49 (MMO + persistent world)
- **Persistent world state across players** — M49
- **Faction-vs-faction war (large scale)** — M49
- **Cross-server events** — M49

### Moved to T-ACC-PLUS (BP9+) accessibility extension
- **Tier-A 11-language localization** — M8 reserves ICU surface; T-ACC-PLUS fills 10 languages
- **Screen reader support + ARIA tags** — T-ACC-PLUS
- **Eye-tracking accessibility** — T-ACC-PLUS
- **Cognitive accessibility presets** — T-ACC-PLUS

### Moved to M8A (parallel-deterministic ECS + GPU offload + networking foundation)
- **Cross-platform float determinism CI matrix** — M8A fills (`game/scripts/ci/m8a_cross_os_determinism.sh` runs Linux x86_64 + macOS aarch64 + Windows x86_64 and diffs final checksums). The original "BP6+ fills" routing was wrong; M8A is the milestone that operationalizes DR-052.
- **GPU compute acceleration for cosmetic systems** — M8A fills for cosmetic particles + material overlay (compute shader + fragment shader). The active-material CA stays CPU-side per the M8A determinism contract; GPU-compute CA remains BP6+ research.
- **Per-subsystem perf-budget profiling + regression gate** — M8A fills (`summary.json.performance` p50/p99/p999 + Tracy/Puffin behind feature flags + `m8a_perf_gate.sh` CI gate).

### Moved to BP6+ engineering optimization
- **Replay format compression (zstd / lz4 streaming)** — BP6+
- **Replay schema migration tools** — BP6+
- **GPU compute acceleration for active material simulation (M15 CA on GPU)** — BP6+ (M15 ships CPU-side parallel CA per M8A's determinism mandate; cross-vendor GPU reproducibility is unsolved)
