# Handoff

Working state: 49 spec files were renamed to `M1..M49` in the working tree (uncommitted). The new filenames are at `specs/done/M1..M3.md` and `specs/active/M4..M49.md`. **Content inside the files is NOT updated to match the new names** — titles, cross-references, dependency lists, and closure-procedure paths still use legacy ids (M5.7, M2.5, M0.5, etc).

Your job:

1. Re-read every line of every milestone in `specs/done/` and `specs/active/`. No exceptions.
2. Rename the milestones in the order they should be executed for best development.
3. If content from one milestone belongs in another, move it now.
4. Re-read each milestone and ensure all cross-references are accurate.

Do it your way. Commit when done.
